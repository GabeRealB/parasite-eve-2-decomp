#include "common.h"

/// Empty task state: the middle entry of the room task's state table. Its only
/// trace is a 0x10-byte stack frame.
void func_dryfield_night_driveway_8017DD7C(void)
{
    char pad[0x10];
}

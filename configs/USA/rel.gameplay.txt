// Reloc overrides for the gameplay overlay.
// Add entries here when splat misidentifies a reloc (same format as rel.main.txt).
//
// These instructions are %hi/%lo of a symbol in the original object, but the
// matching C emits a numeric immediate (so GCC can fill a C branch delay slot;
// asm("%hi")/"%lo" will not). Linked bytes are identical. Mark them MIPS_NONE
// so objdiff expected objects match the compiled objects.

//--------------------------------------------------------------------
//--- VA: 0x800BC50C   destHi = 0x80110000; dest = destHi + (s16)0xD62C
//--------------------------------------------------------------------
rom:0x28D84 reloc:MIPS_NONE symbol:Gp_MoveScanDst
rom:0x28DB8 reloc:MIPS_NONE symbol:Gp_MoveScanDst

//--------------------------------------------------------------------
//--- VA: 0x800C9E94   lui %hi(Mc_SaveData+0x5BC) in bnez delay
//--------------------------------------------------------------------
rom:0x366E4 reloc:MIPS_NONE symbol:Mc_SaveData

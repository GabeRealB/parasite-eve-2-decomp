// Reloc overrides for the actor overlays.
//
// A %hi in a branch delay slot whose %lo sits after the branch: splat cannot
// pair them and emits a literal address. That bakes one RAM slot's value into a
// body shared between slots, so it has to be forced back into a relocation.
rom:0x1F30 reloc:MIPS_HI16 symbol:Actor02000_D03784
rom:0x1F68 reloc:MIPS_LO16 symbol:Actor02000_D03784

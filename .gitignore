asm/
/assets
build/
dumps/
rom/
linkers/
local/
__pycache__/
permuter/
nonmatchings/
tools/giveups/
tools/permuter_findings/
expected/
.cache/
.splat/
.splache
.ninja_log
.ninja_deps
*.ninja

tools/vacuum.log
tools/vacuum-*.log
tools/vacuum.log.lock
ctx.c
ctx.c.m2c
debug_source.c
debug_compiled_object.o
target_object_dump.s
.DS_Store
objdiff.json
compile_commands.json
.venv*
venv*

# Per-worktree sweep scaffolding. These must be ignored in their symlink/file
# form too, not just as directories: a lane worktree links assets/ and venv/ to
# the trunk, and a directory-only pattern left the symlinks untracked-but-not-
# ignored, so an agent's `git add -A` committed them. Landing that on trunk
# replaced the real assets/ and venv/ with symlinks to themselves - every
# venv/bin/python3 then failed with ELOOP and the next landing could not take
# the merge lock.
/.split.log
/OVERLAY_BRIEF.md

# peassets ladder logs (regenerated each run)
tools/peassets/logs/

# Generated splat configs (tools/gen_overlay_configs.py)
configs/USA/generated/
.bulk_m2c/
.promote-logs/

# Claude Code session state (agent worktrees, local settings)
.claude/
